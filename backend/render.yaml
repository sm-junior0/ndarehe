services:
  - type: web
    name: ndarehe-backend
    env: node
    buildCommand: node build-production.js
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000 